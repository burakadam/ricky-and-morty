import { createTheme } from '@mui/material';

const THEME = createTheme({
  palette: {
    primary: {
      main: '#1a1a1a'
    },
    secondary: {
      main: '#f3f3f3'
    }
  }
});

export default THEME;

import styled from '@emotion/styled';
import { Link } from 'react-router-dom';

const Nav = styled(Link)`
  position: absolute;
  height: 100%;
  width: 100%;
  left: 0;
  right: 0;
  z-index: 10;
`;

export { Nav };
